{
  "name": "@solana-peg-risk/tests-trigger",
  "version": "0.1.0",
  "description": "Haiku trigger harness for the peg-risk skill (requires ANTHROPIC_API_KEY)",
  "type": "module",
  "scripts": {
    "test:trigger": "npx tsx run.ts",
    "test:trigger:verbose": "npx tsx run.ts --verbose"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "tsx": "^4.19.0"
  }
}
